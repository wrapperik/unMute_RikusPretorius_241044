# Production environment variables for Vite build
# This tells the frontend where to make API calls in production
# In production, backend serves both frontend and API on same domain
# So /api routes to the Express backend automatically (no localhost:5050)
VITE_API_BASE=/api
