# ====================================
# ENVIRONMENT VARIABLES - LOCAL SETUP
# ====================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git (it's in .gitignore)

# Node Environment
NODE_ENV=development

# Database Configuration - LOCAL (via Cloud SQL Proxy)
# When running locally, use Cloud SQL Proxy on port 3307
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=your_cloud_sql_username
DB_PASS=your_cloud_sql_password
DB_NAME=unmute_db
DB_SSL=false

# JWT Secret for authentication tokens
# Generate a secure random string for production
JWT_SECRET=your-local-jwt-secret-change-this

# Server Port (optional, defaults to 5050)
PORT=5050

# ====================================
# PRODUCTION SETUP (Heroku)
# ====================================
# Set these in Heroku using:
# heroku config:set VARIABLE=value --app unmute
#
# NODE_ENV=production
# DB_HOST=<Your Cloud SQL Public IP>
# DB_PORT=3306
# DB_USER=<Your Cloud SQL username>
# DB_PASS=<Your Cloud SQL password>
# DB_NAME=unmute_db
# DB_SSL=true
# JWT_SECRET=<Generate a strong random secret>
# PORT=<Heroku sets this automatically>
